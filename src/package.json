{
  "name": "discord-bot",
  "version": "1.1.12",
  "description": "a bot to listen for user logins on a minecraft server",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon app.js",
    "test": "node --test",
    "run": "node app.js",
    "git_all": "npm version patch --no-git-tag-version && git commit -a -m 'general update' && git push"
  },
  "author": "hrHVN",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^14.22.1",
    "dotenv": "^16.4.7",
    "mcquery": "^1.0.3"
  },
  "devDependencies": {
    "@sinonjs/fake-timers": "^15.0.0",
    "nodemon": "^3.1.10",
    "sinon": "^21.0.0"
  }
}
